apiVersion: v1
kind: Service
metadata:
    name: camiloestradacalidaddatosfront
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: camiloestradacalidaddatosfront